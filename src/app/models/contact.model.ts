export class Contact {
    stockage: string;
    emballage: string;
    livraison: string;
    service: string;
    recrutement: string;
    date: string;
    constructor(public name: string, public email: string) {
    }
  }